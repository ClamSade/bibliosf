{% extends 'base.html.twig' %}

{% block title %}Résultat de la recherche!{% endblock %}

{% block body %}
    <h1>Résultat de la recherche de : <code>{{ mot }}</code></h1>
    <div class="card-columns">
        {% for livre in livres %}

            {{  include('livre/vignette.html.twig') }}

        {% endfor %}
    </div>
{% endblock %}
